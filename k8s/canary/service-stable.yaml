apiVersion: v1
kind: Service
metadata:
  name: project-veil-server-stable
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: project-veil-server
    app.kubernetes.io/track: stable
  ports:
    - name: http
      port: 80
      targetPort: http
