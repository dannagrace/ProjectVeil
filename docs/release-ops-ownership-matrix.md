# Release-Ops Ownership Matrix

Generated from the maintained release script inventory. Do not edit this file by hand; update `scripts/release-ops-ownership-matrix.ts` and regenerate it.

Covered scripts: 39

## How To Use This Matrix

- Open this page when reviewers ask which command or artifact is authoritative for a release/readiness question.
- `Decision role` distinguishes the canonical gate from supporting evidence and reviewer-facing diagnostics.
- `Review treatment` makes the blocker boundary explicit: use `merge/release blocker` for changes that can stop both PR approval and candidate promotion, `release blocker` for candidate-only proof, and `review aid` for summaries that should not override the owning gate.

## Summary

| Command | Responsibility | Owner | Decision role | Review treatment |
| --- | --- | --- | --- | --- |
| `release:candidate-triage:digest` | same-candidate | release ops | diagnostic | review aid |
| `release:candidate:evidence-audit` | same-candidate | release ops | authoritative gate | release blocker |
| `release:cocos-rc:bundle` | wechat-release | primary-client owner | required evidence | release blocker |
| `release:cocos-rc:snapshot` | wechat-release | primary-client owner | required evidence | release blocker |
| `release:cocos:primary-diagnostics` | candidate-level | primary-client owner | required evidence | release blocker |
| `release:cocos:primary-journey-evidence` | candidate-level | primary-client owner | required evidence | merge/release blocker |
| `release:evidence:index` | same-revision | release ops | required evidence | release blocker |
| `release:gate:summary` | candidate-level | release ops | authoritative gate | merge/release blocker |
| `release:go-no-go-packet` | candidate-level | release ops | authoritative gate | release blocker |
| `release:health:summary` | review-aid | release ops | diagnostic | review aid |
| `release:health:trend-baseline` | review-aid | release ops | diagnostic | review aid |
| `release:health:trend-compare` | review-aid | release ops | diagnostic | review aid |
| `release:phase1:candidate-dossier` | candidate-level | candidate owner | required evidence | release blocker |
| `release:phase1:candidate-rehearsal` | candidate-level | candidate owner | required evidence | release blocker |
| `release:phase1:exit-audit` | candidate-level | release ops | authoritative gate | release blocker |
| `release:phase1:same-revision-evidence-bundle` | same-revision | candidate owner | required evidence | release blocker |
| `release:pr-summary` | review-aid | release ops | diagnostic | review aid |
| `release:readiness:dashboard` | review-aid | release ops | diagnostic | review aid |
| `release:readiness:snapshot` | same-revision | release ops | authoritative gate | merge/release blocker |
| `release:reconnect-soak` | runtime | runtime owner | required evidence | release blocker |
| `release:runtime-observability:evidence` | runtime | runtime owner | required evidence | release blocker |
| `release:runtime-observability:gate` | runtime | runtime owner | authoritative gate | release blocker |
| `release:runtime:slo-summary` | runtime | runtime owner | diagnostic | review aid |
| `release:same-candidate:evidence-audit` | same-candidate | release ops | authoritative gate | release blocker |
| `release:wechat:rehearsal` | wechat-release | wechat release owner | required evidence | release blocker |
| `smoke:client:release-candidate` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `smoke:cocos:canonical-journey` | candidate-level | primary-client owner | required evidence | merge/release blocker |
| `smoke:wechat-release` | wechat-release | wechat release owner | required evidence | release blocker |
| `validate:analytics-schema` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:assets` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:battle` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:content-pack` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:content-pack:all` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:content-smoke` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:e2e:fixtures` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:quickstart` | candidate-level | candidate owner | required evidence | merge/release blocker |
| `validate:redis-scaling` | runtime | runtime owner | required evidence | merge/release blocker |
| `validate:wechat-build` | wechat-release | wechat release owner | required evidence | merge/release blocker |
| `validate:wechat-rc` | wechat-release | wechat release owner | authoritative gate | release blocker |

## Candidate-Level Evidence

These commands own the proof for one release candidate or its directly blocking sub-gates.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:cocos:primary-diagnostics` | primary-client owner | required evidence | `artifacts/release-readiness/cocos-primary-diagnostic-snapshots-<short-sha>-<timestamp>.json` | Checkpoint diagnostics that support the primary-client release call. Treat missing checkpoints as a release blocker when reviewers need shipped-client evidence. |
| `release:cocos:primary-journey-evidence` | primary-client owner | required evidence | `artifacts/release-readiness/cocos-primary-journey-evidence-<candidate>-<short-sha>.json` | Canonical primary-client journey proof for player-facing release readiness. Missing or failed evidence blocks release-facing changes on the shipped client path. |
| `release:gate:summary` | release ops | authoritative gate | `artifacts/release-readiness/release-gate-summary-<short-sha>.json` | Top-level release gate summary. Treat failures as release blockers and use this as the canonical answer when reviewers ask whether the candidate can advance. |
| `release:go-no-go-packet` | release ops | authoritative gate | `artifacts/release-readiness/go-no-go-decision-packet-<candidate>-<short-sha>.json` | Final candidate decision packet. Missing or failed packet inputs block candidate promotion even if individual supporting artifacts exist elsewhere. |
| `release:phase1:candidate-dossier` | candidate owner | required evidence | Bundle directory `artifacts/release-readiness/phase1-candidate-dossier-<candidate>-<short-sha>/` with `phase1-candidate-dossier.json/.md`, `runtime-observability-dossier.json/.md`, `release-gate-summary.json/.md`, and `release-health-summary.json/.md`. | Candidate-level packet that assembles the release story for reviewers. Treat stale or failed packet assembly as blocking candidate review. |
| `release:phase1:candidate-rehearsal` | candidate owner | required evidence | Bundle directory under `artifacts/release-readiness/phase1-candidate-rehearsal/` with staged JSON/Markdown outputs and a top-level `SUMMARY.md`. | Candidate-level packet that assembles the release story for reviewers. Treat stale or failed packet assembly as blocking candidate review. |
| `release:phase1:exit-audit` | release ops | authoritative gate | `artifacts/release-readiness/phase1-exit-audit-<candidate>-<short-sha>.json` | Reviewer-facing Phase 1 exit call. Treat failed or stale criteria as blocking for Phase 1 candidate promotion and exit review. |
| `smoke:client:release-candidate` | candidate owner | required evidence | `artifacts/release-readiness/client-release-candidate-smoke-<short-sha>-<timestamp>.json` | Packaged H5 smoke proof for the release candidate. A failed or missing report blocks top-level gate aggregation for candidate promotion. |
| `smoke:cocos:canonical-journey` | primary-client owner | required evidence | `artifacts/release-readiness/cocos-primary-journey-evidence-<candidate>-<short-sha>.json` | Canonical primary-client journey proof for player-facing release readiness. Missing or failed evidence blocks release-facing changes on the shipped client path. |
| `validate:analytics-schema` | candidate owner | required evidence | No tracked artifact; exits non-zero on schema drift and reports findings on stdout/stderr. | Blocking validation for the touched surface. Treat failures as merge blockers; include them in release review when the candidate depends on the edited contract or workflow. |
| `validate:assets` | candidate owner | required evidence | No tracked artifact; exits non-zero when asset metadata, coverage, or release-ready checks fail. | Blocking validation for shipped content/assets. Failures block merge for release-facing content changes and block candidate promotion when the surface is in scope. |
| `validate:battle` | candidate owner | required evidence | No tracked artifact; prints the validation summary to stdout. | Blocking validation for the touched surface. Treat failures as merge blockers; include them in release review when the candidate depends on the edited contract or workflow. |
| `validate:content-pack` | candidate owner | required evidence | Optional JSON report at the requested `--report-path`; otherwise this is an exit-code-only validation step. | Blocking validation for shipped content/assets. Failures block merge for release-facing content changes and block candidate promotion when the surface is in scope. |
| `validate:content-pack:all` | candidate owner | required evidence | Optional JSON report at the requested `--report-path`; otherwise this is an exit-code-only validation step. | Blocking validation for shipped content/assets. Failures block merge for release-facing content changes and block candidate promotion when the surface is in scope. |
| `validate:content-smoke` | candidate owner | required evidence | No tracked artifact; exits non-zero when the content smoke gate detects missing or invalid shipped content. | Blocking validation for shipped content/assets. Failures block merge for release-facing content changes and block candidate promotion when the surface is in scope. |
| `validate:e2e:fixtures` | candidate owner | required evidence | No tracked artifact; exits non-zero when fixture metadata drifts. | Blocking validation for the touched surface. Treat failures as merge blockers; include them in release review when the candidate depends on the edited contract or workflow. |
| `validate:quickstart` | candidate owner | required evidence | No tracked artifact; this is a workflow gate driven by exit status and console output. | Blocking validation for the touched surface. Treat failures as merge blockers; include them in release review when the candidate depends on the edited contract or workflow. |

## Same-Revision Evidence

These commands pin one coherent revision packet so reviewers are not comparing mixed evidence sets.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:evidence:index` | release ops | required evidence | `artifacts/release-readiness/current-release-evidence-index-<short-sha>.json` | Indexes the current revision artifact families. Use it to prove the packet is complete before deeper review; missing required families should stop release review. |
| `release:phase1:same-revision-evidence-bundle` | candidate owner | required evidence | Output directory under `artifacts/release-readiness/phase1-same-revision-evidence-bundle-<candidate>-<short-sha>/` containing `phase1-same-revision-evidence-bundle-manifest.json`, `phase1-same-revision-evidence-bundle.md`, and staged evidence artifacts. | Assembles one revision-scoped packet for Phase 1 review. Treat drift or missing staged artifacts as a release blocker for same-revision review flows. |
| `release:readiness:snapshot` | release ops | authoritative gate | `artifacts/release-readiness/release-readiness-<timestamp>.json` | Baseline same-revision release record. Required automated/manual checks must pass or be explicitly pending here before downstream gate aggregation is trustworthy. |

## Same-Candidate Consistency

These commands audit whether independently produced artifacts still describe the same candidate/revision pair.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:candidate-triage:digest` | release ops | diagnostic | `artifacts/release-readiness/candidate-revision-triage-digest-<candidate>-<revision>.json` | Triage rollup for candidate discussion. Useful for reviewer handoff, but it does not replace the same-candidate audit or top-level gate summary. |
| `release:candidate:evidence-audit` | release ops | authoritative gate | `artifacts/release-readiness/candidate-evidence-audit-<candidate>-<short-sha>.json` | Canonical same-candidate consistency audit. Any freshness, revision, or candidate mismatch is a release blocker until the artifact set is refreshed. |
| `release:same-candidate:evidence-audit` | release ops | authoritative gate | `artifacts/release-readiness/candidate-evidence-audit-<candidate>-<short-sha>.json` | Canonical same-candidate consistency audit. Any freshness, revision, or candidate mismatch is a release blocker until the artifact set is refreshed. |

## Runtime And Observability

These commands own live-runtime, reconnect, and operational health evidence that can block promotion.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:reconnect-soak` | runtime owner | required evidence | `artifacts/release-readiness/colyseus-reconnect-soak-summary-<candidate-or-short-sha>.json` | Required blocking evidence for reconnect and room-recovery release changes. Fail closed on missing cleanup, failed reconnect cycles, or stale candidate evidence. |
| `release:runtime-observability:evidence` | runtime owner | required evidence | `artifacts/release-readiness/runtime-observability-evidence-<candidate-or-short-sha>-<short-sha>.json` | Raw runtime capture that feeds the runtime observability gate. Required whenever live health, auth-readiness, or metrics evidence is part of the release call. |
| `release:runtime-observability:gate` | runtime owner | authoritative gate | `artifacts/release-readiness/runtime-observability-gate-<candidate-or-short-sha>.json` | Canonical runtime observability gate. A failed or stale gate blocks promotion for runtime-facing candidates and WeChat release review. |
| `release:runtime:slo-summary` | runtime owner | diagnostic | `artifacts/release-readiness/runtime-slo-summary-<short-sha>.json` | SLO-oriented summary for operational review. Use it to explain posture and regressions, but not as the authoritative release decision on its own. |
| `validate:redis-scaling` | runtime owner | required evidence | No tracked artifact; exits non-zero if the scaling validation fails. | Blocking validation for Redis-backed runtime scaling changes. Treat failures as merge blockers for scaling work and release blockers when the candidate depends on that path. |

## WeChat Release Evidence

These commands own the WeChat export, package, smoke, and candidate-summary proof required for the shipped surface.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:cocos-rc:bundle` | primary-client owner | required evidence | Bundle files under `artifacts/release-readiness/`, including `cocos-rc-evidence-bundle-<candidate>-<short-sha>.json` and companion Markdown/checklist/blocker artifacts. | Primary-client RC evidence for the Cocos/WeChat release surface. Required whenever reviewers need the shipped-client packet, checklist, and blockers log. |
| `release:cocos-rc:snapshot` | primary-client owner | required evidence | `artifacts/release-evidence/cocos-rc-snapshot-<timestamp>.json` by default, or validation of an existing snapshot when `--check` is used. | Primary-client RC evidence for the Cocos/WeChat release surface. Required whenever reviewers need the shipped-client packet, checklist, and blockers log. |
| `release:wechat:rehearsal` | wechat release owner | required evidence | Summary files under the selected artifacts dir, typically `artifacts/wechat-release/wechat-release-rehearsal-<candidate>.json` and `.md`, alongside the package, validation, smoke, and upload artifacts it references. | WeChat rehearsal packet that proves the prepare/package/verify flow can run coherently. Treat failed rehearsal output as a release blocker for RC packaging review. |
| `smoke:wechat-release` | wechat release owner | required evidence | `codex.wechat.smoke-report.json` in the selected artifacts directory, or validation of an existing smoke report when `--check` is used. | Device or quasi-device WeChat smoke evidence. Required supporting proof for WeChat release readiness even when the top-level gate is produced elsewhere. |
| `validate:wechat-build` | wechat release owner | required evidence | No tracked artifact; exits non-zero when the build directory or exported runtime shape is invalid. | Blocking export/build validator for the WeChat runtime surface. Failures block merge for export changes and block release packaging review. |
| `validate:wechat-rc` | wechat release owner | authoritative gate | `codex.wechat.rc-validation-report.json` in the selected artifacts dir | Canonical WeChat candidate validation and summary. Missing, blocked, or failed output is a release blocker for the shipped WeChat surface. |

## Review Aids And Diagnostics

These commands help reviewers triage or summarize release posture, but they are not the canonical release decision by themselves.

| Command | Owner | Decision role | Canonical artifacts | Blocking semantics |
| --- | --- | --- | --- | --- |
| `release:health:summary` | release ops | diagnostic | `artifacts/release-readiness/release-health-summary-<short-sha>.json` | Reviewer-facing summary or trend aid. Use it to explain release posture and next steps, but do not treat it as the source of truth over the owning gates. |
| `release:health:trend-baseline` | release ops | diagnostic | `artifacts/release-readiness/release-health-trend-baseline.json` | Reviewer-facing summary or trend aid. Use it to explain release posture and next steps, but do not treat it as the source of truth over the owning gates. |
| `release:health:trend-compare` | release ops | diagnostic | `artifacts/release-readiness/release-health-trend-compare.json` | Reviewer-facing summary or trend aid. Use it to explain release posture and next steps, but do not treat it as the source of truth over the owning gates. |
| `release:pr-summary` | release ops | diagnostic | `artifacts/release-readiness/release-pr-comment.md` | Reviewer-facing summary or trend aid. Use it to explain release posture and next steps, but do not treat it as the source of truth over the owning gates. |
| `release:readiness:dashboard` | release ops | diagnostic | `artifacts/release-readiness/release-readiness-dashboard.json` | Reviewer-facing summary or trend aid. Use it to explain release posture and next steps, but do not treat it as the source of truth over the owning gates. |
